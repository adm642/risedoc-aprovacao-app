/**
 * Dashboard da agência (autenticado).
 * Ref: Projetos/Spec UI — Painel da Agência (vault)
 */
export default function DashboardPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-3 p-8 text-center">
      <h1 className="font-display text-2xl font-bold text-charcoal-900">
        Painel da Agência
      </h1>
      <p className="text-sm text-charcoal-900/60">
        Fundação criada. Em construção — implementar a partir do protótipo aprovado.
      </p>
    </main>
  );
}
